# use by adding anything to git and then do:
# - heroku login
# - heroku stack:set container
# - git push heroku HEAD:master
setup:
   addons:
     - plan: heroku-postgresql
       as: database
build:
   docker:
       php: heroku-php/Dockerfile
run:
   php:
       command:
          - php /var/www/include/php/index.php
